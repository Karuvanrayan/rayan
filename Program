import numpy as np
import seaborn as sns
import matplotlib.pyplot as plt
import pandas as pd



df= pd.read_csv("/content/delhiaqi.csv")



df.info()

df.isnull().sum()

df.head(6)

df.columns

data2=df.copy()

dist=(data2['Data'])
distset=set(dist)
dd=list(distset)
world={dd[i]:i for i in range(len(dd))}
data2['Data']=data2['Data'].map(world)


dist=(data2['AQL'])
distset=set(dist)
dd=list(distset)
world={dd[i]:i for i in range(len(dd))}
data2['AQL']=data2['AQL'].map(world)


data2["AQL"]=data2["AQL"].fillna(data2["AQL"])
data2.isnull().sum()

import plotly.express as px
import pandas as pd
fig=px.scatter(df,x="Data",y="AQL")
fig.show()


import plotly.express as px
import pandas as pd
fig=px.scatter(df,x="Data",y="AQL")
fig.show()


import plotly.express as px
import pandas as pd
fig2=px.scatter(df,x="PM10",y="AQL")
fig2.show()

import plotly.express as px
import pandas as pd
fig3=px.scatter(df,x="NO2",y="AQL")
fig3.show()

import plotly.express as px
import pandas as pd
fig4=px.scatter(df,x="NO",y="AQL")
fig4.show()

import plotly.express as px
import pandas as pd
fig5=px.scatter(df,x="CO",y="AQL")
fig5.show()

import plotly.express as px
import pandas as pd
fig6=px.scatter(df,x="O3",y="AQL")
fig6.show()

import plotly.express as px
import pandas as pd
fig7=px.scatter(df,x="SO2",y="AQL")
fig7.show()

import plotly.express as px
import pandas as pd
fig8=px.scatter(df,x="PM2.5",y="AQL")
fig8.show()

data2.columns

features=data2[['Data', 'CO', 'NO', 'NO2', 'O3', 'SO2', 'PM2.5', 'PM10', 'AQL']]
labels=data2['AQL']

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test=train_test_split(features,labels,test_size=0.2,random_state=2)

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.metrics import classification_report
from sklearn import metrics
from sklearn import tree


from sklearn.ensemble import RandomForestRegressor
from sklearn.datasets import make_regression
regr=RandomForestRegressor(max_depth=2,random_state=0)
regr.fit(x_train,y_train)
print(regr.predict(x_test))

y_pred=regr.predict(x_test)

from sklearn.metrics import r2_score
r2_score(y_test,y_pred)
